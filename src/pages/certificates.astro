---
import Base from '../layouts/Base.astro';
import CredentialCard from '../components/CredentialCard.astro';
import { getCredentials } from '../lib/content';

const credentials = await getCredentials();
const certifications = credentials.filter((c) => c.data.kind === 'certification');
const certificates = credentials.filter((c) => c.data.kind === 'certificate');
---

<Base title="Certificates" description="Certifications, courses and workshops completed by Abdulrhman Salamah.">
  <header class="py-10" data-reveal>
    <p class="font-mono text-xs tracking-[0.2em] text-accent-2">// CREDENTIALS</p>
    <h1 class="mt-2 text-4xl font-black tracking-tight sm:text-5xl">
      <span class="gradient-text">Certificates</span>
    </h1>
    <p class="mt-3 max-w-2xl text-muted">
      Formal certifications first, then course and workshop certificates.
    </p>
  </header>

  {
    credentials.length === 0 && (
      <p class="rounded-xl border border-dashed border-border p-8 text-center text-muted">
        Nothing published here yet.
      </p>
    )
  }

  {
    certifications.length > 0 && (
      <section class="mt-4">
        <h2 class="font-mono text-xs tracking-[0.2em] text-accent" data-reveal>CERTIFICATIONS</h2>
        <div class="mt-4 grid gap-4">
          {certifications.map((credential, i) => (
            <div data-reveal style={`--reveal-delay:${i * 80}ms`}>
              <CredentialCard credential={credential} />
            </div>
          ))}
        </div>
      </section>
    )
  }

  {
    certificates.length > 0 && (
      <section class="mt-12">
        <h2 class="font-mono text-xs tracking-[0.2em] text-accent" data-reveal>
          COURSES AND WORKSHOPS
        </h2>
        <div class="mt-4 grid gap-4">
          {certificates.map((credential, i) => (
            <div data-reveal style={`--reveal-delay:${Math.min(i, 6) * 60}ms`}>
              <CredentialCard credential={credential} />
            </div>
          ))}
        </div>
      </section>
    )
  }
</Base>
